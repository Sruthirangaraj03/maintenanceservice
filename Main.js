/* import { Router } from 'express'
import React from 'react'
import { Route, Routes } from 'react-router-dom'
import LoginPage from './LoginPage'
import App from './App'

const Main = () => {
  return (
    <>
    <Router>
        <Routes>
            <Route to="/" element={<LoginPage />} />
            <Route to="/dashbaord" element={<App />} />
        </Routes>
    </Router>
    </>
  )
}

export default Main; */
